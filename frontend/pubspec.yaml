name: netflow
description: Personal finance ledger — Flutter frontend.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: "2.11.1"
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1
  intl: ^0.19.0
  fl_chart: ^0.69.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
